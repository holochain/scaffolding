[package]
name = "hello_world"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
name = "hello_world"

[dependencies]
hdk = { workspace = true }

serde = { workspace = true }

hello_world_integrity = { workspace = true } 

# The following two lines should be removed when upgrading to Holochain 0.4
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
home = { workspace = true }
