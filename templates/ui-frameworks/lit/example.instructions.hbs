Run the example app with:

  cd {{example}}
  nix develop
  {{(package_manager_command "install" null)}}
  {{(package_manager_command "start" null)}}

Generated ui code might also need to be reformatted:

  {{(package_manager_command "format" "ui")}}