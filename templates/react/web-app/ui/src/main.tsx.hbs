import React from 'react'
import ReactDOM from 'react-dom/client'

import App from './App.tsx'
import HolochainProvider from './contexts/HolochainContext.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HolochainProvider>
      <App />
    </HolochainProvider>
  </React.StrictMode>,
)
