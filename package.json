{
  "name": "@holochain/scaffolding-dev",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "bin": "./packages/create/dist/hc-scaffold.js",
  "scripts": {
    "start": "concurrently -k \"yarn workspace @holochain/scaffolding-ui build:watch && yarn workspace @holochain/scaffolding-generators run build:watch && yarn workspace @holochain/scaffolding-types run build:watch\" \"yarn workspace @holochain/create run start\"",
    "postinstall": "npm run build",
    "test": "yarn workspace @holochain/scaffolding-generators run test",
    "build": "yarn workspace @holochain/scaffolding-generators run build && yarn workspace @holochain/scaffolding-types run build && yarn workspace @holochain/scaffolding-ui run build && yarn workspace @holochain/create run build"
  },
  "devDependencies": {
    "chalk": "^4.1.2",
    "concurrently": "^6.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.3"
  }
}
