{
  "name": "@holochain/create-monorepo",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "./packages/client",
    "./packages/server",
    "./packages/generators"
  ],
  "scripts": {
    "start": "npm run build -w @holochain/create-ui && concurrently -k \"npm run build:watch -w @holochain/create-ui\" \"npm start -w @holochain/create\""
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
