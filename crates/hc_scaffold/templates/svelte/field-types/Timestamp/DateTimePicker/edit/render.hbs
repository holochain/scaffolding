<vaadin-date-time-picker label="{{label}}" {{#if initial_value_variable}}value={new Date({{initial_value_variable}} / 1000).toISOString()}{{/if}} on:change={e => { {{variable_to_change}} = new Date(e.target.value).valueOf() * 1000;} } {{#if required}}required{{/if}}></vaadin-date-time-picker>

