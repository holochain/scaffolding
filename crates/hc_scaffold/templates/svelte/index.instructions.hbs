{{#if (eq index_type.type "Global")}}
At first, the UI for this application is empty. If you want the newly scaffolded index to be the entry point for its UI, import the element in `ui/src/App.svelte`:

  import {{pascal_case index_name}} from './{{dna_role_id}}/{{coordinator_zome_manifest.name}}/{{pascal_case index_name}}.svelte';
  
And insert it in the `<div id="content" />` like this:

  <div id="content"><{{pascal_case index_name}}></{{pascal_case index_name}}></div>
{{/if}}
