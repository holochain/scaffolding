{
  "name": "@holochain-scaffolding/definitions",
  "version": "0.0.1",
  "description": "Common types for the client and the server to communicate with each other",
  "author": "guillem.cordoba@gmail.com",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "start": "tsc -w",
    "build": "rimraf dist && tsc",
    "build:watch": "tsc -w --preserveWatchOutput"
  },
  "dependencies": {
    "@holochain-scaffolding/vocabulary": "^0.0.1",
    "@type-craft/rust": "^0.0.1",
    "@source-craft/cargo": "^0.0.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.4.3"
  },
  "files": [
    "dist"
  ]
}
